import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rick&morty@graphql',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
